- [Square UI dashboard-4 design](square-ui-design.md) — oklch palette, dark by default, rounded content card layout, stats card nested box pattern.
- [sulsel_desa GeoJSON coordinate normalization](sulsel-geojson-normalization.md) — 95 features have extra nesting level in Polygon coordinates; must be normalized before passing to Leaflet.
- [React Leaflet HMR effect issue](react-leaflet-hmr.md) — Leaflet layers inside MapContainer: use conditional mount ({cond && <Layer />}) not visible prop, so HMR resets effect properly.
- [SLIS Calculation Engine separation](slis-calc-engine.md) — AI must not generate financial numbers; Calculation Engine computes all metrics, AI writes narrative only. Key pattern for all future AI analysis features.
- [Satara Dashboard API route prefix bug](satara-api-routes.md) — app.use("/api", router) pattern means route files must NOT include /api/ prefix. HR routes had "/api/hr/" causing double-prefix 404s.
- [Satara Dashboard seed/schema mismatches](satara-seed-schema.md) — OTS checked status==="done" but seed uses "completed"; productivity table has no employee_count column; competency table uses "competency_name" not "name"; employees use "employment_status" not "is_active". Always verify enum values match between seed and API.
- [HR routes try/catch pattern](hr-routes-error-handling.md) — hr.ts had NO try/catch on any route; Express 5 returned HTML on unhandled errors; frontend .json() threw SyntaxError silently. Fix: err500 helper + try/catch on every route. Also: /bulk routes must come BEFORE base routes.
- [HR frontend apiJson pattern](hr-frontend-fetch-pattern.md) — All HR fetch calls must use shared apiJson from @/lib/api.ts (checks !r.ok, throws Error with server message). Add onError + formError state in mutation forms to surface errors to users.
- [Satara akad-cair multi-disbursement](satara-akad-cair-ledger.md) — ledger table exists; POST /finance/akad-cair/:akadId/pencairan is the correct multi-pencairan endpoint; PATCH is legacy single-disbursement only.
- [Satara feature status reality check](satara-feature-status.md) — auto-reduce kredit, stok validation, and siteplan monitoring in Produksi were all already implemented; gaps were: multi-pencairan frontend, resubmit workflow, land bank fields, lock endpoints.
